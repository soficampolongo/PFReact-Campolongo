import imgCarrito from "./assets/carrito.png";

const CartWidget = (img2) => {
    return(
        <div>
            <img src={imgCarrito} alt="carrito-widget"></img>
            0
        </div>
    )
}

export default CartWidget